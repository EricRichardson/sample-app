<% provide(:title, 'Home') %>
<% if logged_in? %>
  <%= render 'logged_in' %>
  <div class="col-md-8">
      <h3>Micropost Feed</h3>
      <%= render 'shared/feed' %>
    </div>
<% else %>
  <%= render 'logged_out' %>
<% end %>